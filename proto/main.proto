syntax = 'proto3';

message handshake {
  required string session = 1;
}

message close {
  required string id = 1;
}

message ping {
  required uint32 index = 1;
}

message pong {
  required uint32 index = 1;
}
